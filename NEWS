### Version 0.1.2 (8 October 2021)

* corrected a failed test in `set_label.data.frame`. This function now requires that arguments to `...` be named.

### Version 0.1.0 (28 July 2018)

* Added `is.labelled` and its alias `is_labelled`.
* Improved method dispatch to better support vectors with multiple classes.